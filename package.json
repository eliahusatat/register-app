{
  "name": "register-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
    "scripts": {
    "dev:client": "npm run --workspace=@register-app/client dev",
    "dev:server": "npm run --workspace=@register-app/server dev",
    "dev": "npm run dev:client & npm run dev:server"
  },
  "devDependencies": {
    "@types/express": "^5.0.3"
  }
}
